# Week 2 论文总结

## 1. Deep Residual Learning for Image Recognition
### 问题定义
这篇论文要解决的是深层网络退化问题。理论上网络越深，表达能力应该越强，但实际训练时会出现层数加深以后，训练误差反而增大，性能下降。

### 核心方法
resnet框架的核心思想是，不再直接让网络层学习一个期望的映射函数H(x)，而是让这些层学习一个相对于输入的残差映射F(x) := H(x) - x。然后，原始的映射函数可以被重新表述为F(x) + x。
- 这样做的好处是，如果最优的映射函数H(x)接近于输入x，那么残差函数F(x)就接近于0，网络只需要学习一个简单的零映射，这比直接学习一个复杂的恒等映射更容易。
- 反过来说，如果H(x)与x有较大差异，网络仍然可以通过学习一个非零的残差函数F(x)来实现所需的映射。
- 可以理解成：之前的传统网络是在进行重塑，而残差网络是在做修正，相当于打补丁操作。这样深层网络至少不会比浅层网络更差了。

### 与前后工作的关系
ResNet把超深网络训练变成了可行路线。后来的 Transformer、ViT、视觉编码器以及很多多模态模型里大量使用的残差连接，本质上都继承了这篇论文的思想。

### 自己的思考
- 有时候“什么都不做”也很重要。直接把输入 x 传到输出 y，网络只需要学会一个恒等映射（identity mapping）就行了。
- 传统的网络结构像是在强行“重塑”输入，总得训练点什么东西，不能什么都不做。而残差网络更像是在原来的表示上“打补丁”或“做修正”,如果最优情况本来就接近把输入原样传过去，那么残差网络至少能轻松做到这一点。

## 2. Attention Is All You Need

### 问题定义
这篇论文要解决的是传统序列模型在远距离依赖和并行效率上的限制。RNN 需要按前后关系顺序处理，难以并行；CNN 虽然能并行，但难以捕捉远距离依赖。所以作者想不依赖循环和卷积，只靠注意力机制完成序列建模。

### 核心方法
论文提出 Transformer 架构，用 Self-Attention 作为核心，让一个 token 在编码自己时，动态决定应该关注序列中的哪些 token。并通过 Multi-Head Attention让模型能从多个子空间同时建模不同关系，Positional Encoding则补上了位置信息，使完全基于注意力的模型仍能理解顺序。每个 token 先映射成 Q、K、V矩阵，通过 Q 和 K 的相关性计算注意力权重，再对 V 加权求和，得到融合上下文的新表示。

### 自己的思考
它本质上是在做“信息检索”。当前位置需要什么信息，就拿 Query 去整个序列里匹配 Key，再把最有用的 Value 取回来。Query（Q）可以理解成“该token想找什么信息”。Key（K）可以理解成“我这里有什么信息”。Value（V）可以理解成“真正提供的内容”。Multi-Head Attention 也不是简单重复计算，而是让模型并行学不同类型的关系模式。对我来说，这篇论文最重要的启发是：Transformer 之所以能成为多模态大模型的骨架，是因为它擅长并行处理“序列中元素之间的关系”。

## 3. Language Models are Few-Shot Learners 4. LLaMA Technical Report

为什么要用decoder-only结构：由于文本生成任务主要是根据现有信息来预测下一个词，纯解码器专注于这一点，避免了编码器-解码器架构中不必要的复杂性和计算开销。同时，纯解码器结构更适合大规模预训练，可以更高效地利用计算资源。



1. GPT vs BERT
    - GPT是知道之前以及现在的状态去预测开放式的未来；BERT是知道之前和之后的状态去预测被遮挡的中间状态。预测未来比预测中间更难，所以GPT的训练难度更大。而且GPT要训练出一个好的效果，必须要有一个非常大的模型和非常大的数据集，这也是为什么GPT-3有175B参数的原因。

2. GPT-2使用in-context learning
    - GPT-2在做zero-shot任务的时候，对于下游任务，由于不改模型参数，就已经开始使用prompt了。GPT-3在这个基础上进一步发展了few-shot learning的能力，通过在prompt中提供一些示例，让模型靠上下文“临场学习”，从而在没有专门微调的情况下，仍然能够取得不错的性能。

3. GPT-3文章里提到有局限性
    - 大型预训练语言模型缺乏来自其他领域的经验数据，比如视频或现实世界的物理交互数据，因此无法充分理解世界的各种背景信息。这也是为什么后面多模态和世界模型会成为自然的发展方向。

4. llama系列要解决的问题更偏现实工程
    - LLaMA系列技术报告的核心创新不是在于提出了一个全新的架构，而是在相对更小参数规模下通过更多数据训练来获得更高性能。这样在推理时的预算会更低，也更适合开源社区使用。
    - llama后续的框架上的优化主要是使用了分组查询注意力（Grouped Query Attention），它的核心思想是将查询向量分成多个组，每个组独立计算注意力权重，这样可以减少计算量和内存占用，同时保持模型的性能。

5. pre-normlization
    - 在transformer中，层归一化在feedforward之后，是对子层的输出进行归一化。
    - 在gpt以及LLAMA系列中，都使用了pre-normalization的设计，也就是把layer normalization放在子层的前面，对输入进行归一化。这种设计可以让训练更稳定，尤其是在训练非常深的模型时，可以有效缓解梯度消失问题。

6. LayerNorm vs RMSNorm
    - LayerNorm是对每个样本的所有特征进行归一化，计算均值和标准差；RMSNorm则是只计算均方根（Root Mean Square），不计算均值。RMSNorm相对于LayerNorm来说，计算更简单，且在某些情况下可以提供更好的训练稳定性和性能。
    - LayerNorm（GPT‑2/3）：去均值 + 归一化方差 + 偏置
        - $$\text{LN}(x)=\frac{x-\mu}{\sqrt{\sigma^2+\epsilon}}\,\gamma+\beta$$
        - 先减均值 μ（中心化）
        - 再除以标准差 σ（单位方差）
        - 有 γ（缩放）+ β（偏置）

    - RMSNorm（LLaMA、GPT‑4）：只做缩放，不移中心，无偏置
        - $$\text{RMS}(x)=\frac{x}{\sqrt{\mathbb{E}[x^2]+\epsilon}}\,\gamma$$
        - 不减均值，只除以“均方根 RMS”
        - 只有 γ，没有 β

<!-- 
### 问题定义
GPT-3 这篇论文主要要解决的是：模型能不能在不针对单个任务微调参数的情况下，仅凭 prompt 和少量示例就适应新任务。关注的是大语言模型是否能通过上下文直接表现出 few-shot learning 能力。

### 核心方法
GPT-3 延续了 decoder-only Transformer 和自回归训练路线，用大规模模型和大规模数据做统一的语言建模训练。在使用阶段，不改模型参数，而是通过 zero-shot、one-shot、few-shot 的 prompt 方式，把任务描述和示例直接放进上下文，让模型在推理时完成任务。

### 关键创新
它的关键创新并不是提出了全新的结构，而是证明了当模型规模、数据规模和训练计算达到一定程度后，模型会表现出明显更强的 in-context learning 能力。

### 与前后工作的关系
前面的 GPT、GPT-2 已经走在 decoder-only 和自回归预训练这条路上，但 GPT-3 把规模和能力都推到一个新阶段，让 few-shot prompting 成为大模型使用范式的核心。后面的 instruction tuning、chain-of-thought、agent、工具调用等能力，很大程度上都是沿着“如何利用上下文驱动模型完成任务”这条路线继续发展的。

### 自己的思考
结合我的笔记，我现在觉得 GPT 和 BERT 的差别非常关键。GPT 是根据已有信息预测未来，BERT 是根据上下文补中间被遮住的内容，所以 GPT 的训练目标更难，也更依赖超大模型和超大数据。这也解释了为什么 GPT-3 会这么强调规模。另外，我觉得 GPT-3 真正改变大家认知的地方，不只是模型变大了，而是 prompt 这件事开始变得非常重要，模型不改参数也能靠上下文“临场学习”。但它的局限性也很明显，尤其是缺乏来自视频、现实交互等其他模态的经验，这也是为什么后面多模态和世界模型会成为自然的发展方向。



### 问题定义
LLaMA 要解决的问题更偏现实工程：如果没有 GPT-3 那种极端昂贵的训练预算，能不能依然训练出高性能、可复现、而且适合开源社区使用的大语言模型。它关注的是成本、性能和可用性之间的平衡。

### 核心方法
LLaMA 依然走 decoder-only Transformer 路线，但把训练配方做得更精细，包括用更多高质量数据训练相对更小的模型，并在后续版本中继续优化注意力和归一化等细节设计，比如 Grouped Query Attention、pre-normalization、RMSNorm 等。

### 关键创新
它最重要的创新不是架构颠覆，而是证明了“更好的训练配方”本身就是创新。相对更小参数规模下，通过更多数据和更合理的设计，依然可以拿到很强的性能。同时，后续 LLaMA 系列在 GQA、RMSNorm、pre-norm 等工程细节上的优化，也体现了现代大模型越来越像一个系统工程问题。

### 与前后工作的关系
它承接了 GPT 系列的 decoder-only 路线，也吸收了 scaling law 和更优参数量-数据量配比这类经验。和 GPT-3 相比，LLaMA 不再主要强调“规模本身带来能力”，而更强调“在可承受成本下把训练配方做到更优”。它对后续开源 LLM、中文模型和多模态模型的发展影响都非常大。

### 自己的思考
结合我的笔记，我现在觉得 LLaMA 特别能体现“深度学习更像工程问题，而不是理科”这句话。它没有发明一个完全新的框架，但通过更合理的数据、预算和训练细节设计，把整体效果做上去了。LLaMA 系列让我更明确地意识到，大模型能力不只是参数越大越好，还和 scaling law、数据质量、归一化方式、注意力实现细节都有关。像 GQA 是为了减少计算和显存占用，pre-normalization 是为了让深层训练更稳定，RMSNorm 则是在保持效果的同时简化计算。这些点单看都不一定像“革命性创新”，但合在一起就能显著改变一个模型是否真正实用。 -->